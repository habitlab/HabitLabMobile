<Page loaded="pageLoaded"
  xmlns:sidedrawer="shared/sd" 
  xmlns="http://schemas.nativescript.org/tns.xsd" 
  xmlns:sd="nativescript-telerik-ui/sidedrawer" 
  xmlns:taskPie="nativescript-taskpie"
  xmlns:lv="nativescript-telerik-ui/listview"
  navigatingTo="navigatingTo">

	<ActionBar title="HabitLab" class="action-bar">
	   <NavigationButton tap="toggleDrawer" ios.systemIcon="4" android.systemIcon="ic_menu_moreoverflow" ios.position="left" />
	</ActionBar>

	



  <sd:RadSideDrawer id="sideDrawer" drawerContentSize="180" DrawerLength="400">
    <sd:RadSideDrawer.drawerContent>
      <sidedrawer:sd/>
    </sd:RadSideDrawer.drawerContent>
    

	    <sd:RadSideDrawer.mainContent horizontalAlignment="center">

			<TabView>
				 <TabView.items>

					<TabViewItem title="Day" xmlns:dayTab="progressTabs/dayTab" >
					   <TabViewItem.view>
					   		<!-- <dayTab:dayTab/> -->
					   		<StackLayout>
					   			<!-- <Label text="test" /> -->
					   		<!-- 	<taskPie:TaskPie id="pie"
					   			                   counts="{{ taskCounts }}"
					   			                   pieSize="{{ pieSize }}"
					   			                   description="{{ daysLeft ? (daysLeft + ' days left') : null }}"
					   			                   pieText="{{ pie.totalLeft }}" pieSubText="tasks left"
					   			                   countChanged="{{ taskCountChanged }}" />

 -->


					   			<!-- 	For the horizontal button like displays  -->
				   				<lv:RadListView id="listButtons" class="list-group">
				   					<lv:RadListView.listViewLayout>
				   			            <lv:ListViewGridLayout scrollDirection="Vertical" itemHeight="100" spanCount="2"/>
				   			        </lv:RadListView.listViewLayout>
				   			        <lv:RadListView.itemTemplate>
				   			            <StackLayout class="list-buttons">
				   			                <Label text="{{ value }}" class="list-number" />
				   			                <Label text="{{ desc }}" textWrap="true" class="list-desc" />
				   			            </StackLayout>
				   			        </lv:RadListView.itemTemplate>
				   			    </lv:RadListView>


				   			<!-- 
				   			    For the list of goal apps and their time usage -->
				   				<lv:RadListView id="listview" class="list-group">
				   					<lv:RadListView.listViewLayout>
				   			            <lv:ListViewLinearLayout scrollDirection="Vertical"/>
				   			        </lv:RadListView.listViewLayout>
				   			        <lv:RadListView.itemTemplate>
				   			        	<GridLayout class="list-group-item" rows="auto" columns="auto, *, auto">
		   			        	            <Image row="0" col="0" src="{{ image }}" class="thumb img-rounded" />
		   			        	            <StackLayout row="0" col="1" >
		   			        	                <Label text="{{ name }}" class="list-group-item-heading" />
		   			        	                <Label text="{{ 'Opened '+ visits + ' times' }}" class="list-group-item-text" />
		   			        	            </StackLayout>
		   			        	            <Label row ="0" col="2" text="{{ mins + ' mins' }}" class="list-group-item-text"/>
				   			        	</GridLayout>
				   			        </lv:RadListView.itemTemplate>
				   			    </lv:RadListView>

					   		</StackLayout>
						</TabViewItem.view>
					</TabViewItem>
				
					 <TabViewItem title="Week" xmlns:weekTab="progressTabs/weekTab">
						 <TabViewItem.view>
							 <!-- <weekTab:weekTab/> -->
							 <!-- <Label text="test" /> -->
							 <!-- 	For the horizontal button like displays  -->
							 		<lv:RadListView id="weekButtons" class="list-group">
				   					<lv:RadListView.listViewLayout>
				   			            <lv:ListViewGridLayout scrollDirection="Vertical" itemHeight="100" spanCount="2"/>
				   			        </lv:RadListView.listViewLayout>
				   			        <lv:RadListView.itemTemplate>
				   			            <StackLayout class="list-buttons">
				   			                <Label text="{{ value }}" class="list-number" />
				   			                <Label text="{{ desc }}" textWrap="true" class="list-desc" />
				   			            </StackLayout>
				   			        </lv:RadListView.itemTemplate>
				   			    </lv:RadListView>

				   			    <!-- 
				   			    For the list of goal apps and their time usage -->
				   			   <!--  <lv:RadListView id="weekList" class="list-group">
				   					<lv:RadListView.listViewLayout>
				   			            <lv:ListViewLinearLayout scrollDirection="Vertical"/>
				   			        </lv:RadListView.listViewLayout>
				   			        <lv:RadListView.itemTemplate>
				   			        	<GridLayout class="list-group-item" rows="auto" columns="auto, *, auto">
		   			        	            <Image row="0" col="0" src="{{ image }}" class="thumb img-rounded" />
		   			        	            <Label row="0" col="1" text="{{ name }}" class="list-group-item-heading"/>
		   			        	            <StackLayout row ="0" col="2">
			   			        	            <Label text="{{ avgMins }}" class="list-group-item-text"/>
			   			        	            <Label text="avg mins per day" class="list-group-item-text"/>
		   			        	        	</StackLayout>
				   			        	</GridLayout>
				   			        </lv:RadListView.itemTemplate>
				   			    </lv:RadListView> -->



						  </TabViewItem.view>
					  </TabViewItem>
								     
					<TabViewItem title="Month" xmlns:monthTab="progressTabs/monthTab">
						 <TabViewItem.view>
						     <!-- <monthTab:monthTab/> -->
						     <Label text="test" />
				       </TabViewItem.view>

					 </TabViewItem>
				   </TabView.items>
				</TabView>

	    	
	            
	     </sd:RadSideDrawer.mainContent>


  </sd:RadSideDrawer>

</Page>