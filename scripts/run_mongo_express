#!/usr/bin/env node
// Generated by LiveScript 1.5.0
(function(){
  var muri, getsecret, ref$, exec, which, i$, len$, command, parsed, host, port, user, pass;
  muri = require('muri');
  getsecret = require('getsecret');
  ref$ = require('shelljs'), exec = ref$.exec, which = ref$.which;
  for (i$ = 0, len$ = (ref$ = ['mongo-express']).length; i$ < len$; ++i$) {
    command = ref$[i$];
    if (!which(command)) {
      console.log("missing " + command + ". please run the following command:");
      console.log("npm install -g " + command);
      process.exit();
    }
  }
  parsed = muri(getsecret('MONGODB_URI'));
  console.log(parsed);
  ref$ = parsed.hosts[0], host = ref$.host, port = ref$.port;
  ref$ = parsed.auth, user = ref$.user, pass = ref$.pass;
}).call(this);
